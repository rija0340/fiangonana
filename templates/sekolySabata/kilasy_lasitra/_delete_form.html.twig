<form method="post" action="{{ path('kilasy_lasitra_delete', {'id': kilasy_lasitra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kilasy_lasitra.id) }}">
    <button class="btn">Delete</button>
</form>
