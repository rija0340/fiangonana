<!DOCTYPE html>

<title>HiraChoral</title>

{% block body %}
    <h1>HiraChoral</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hira_choral.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ hira_choral.titre }}</td>
            </tr>
            <tr>
                <th>Cle</th>
                <td>{{ hira_choral.cle }}</td>
            </tr>
            <tr>
                <th>Autheur</th>
                <td>{{ hira_choral.autheur }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ hira_choral.createdAt ? hira_choral.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('hira_choral_index') }}">back to list</a>

    <a href="{{ path('hira_choral_edit', {'id': hira_choral.id}) }}">edit</a>

    {{ include('hira_choral/_delete_form.html.twig') }}
{% endblock %}
