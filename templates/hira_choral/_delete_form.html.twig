<form method="post" action="{{ path('hira_choral_delete', {'id': hira_choral.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hira_choral.id) }}">
    <button class="btn">Delete</button>
</form>
