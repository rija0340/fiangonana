<form method="post" action="{{ path('theme_hira_delete', {'id': theme_hira.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme_hira.id) }}">
    <button class="btn">Delete</button>
</form>
