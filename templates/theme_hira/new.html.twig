<!DOCTYPE html>

<title>New ThemeHira</title>

{% block body %}
	<h1>Create new ThemeHira</h1>

	{{ include('theme_hira/_form.html.twig') }}

	<a href="{{ path('theme_hira_index') }}">back to list</a>
{% endblock %}
