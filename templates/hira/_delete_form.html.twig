<form method="post" action="{{ path('hira_delete', {'id': hira.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hira.id) }}">
    <button class="btn">Delete</button>
</form>
